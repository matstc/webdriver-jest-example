{
  "dependencies": {
    "babel-preset-es2015": "^6.24.0",
    "jest": "^19.0.2",
    "jest-cli": "^19.0.2",
    "selenium-webdriver": "^3.4.0"
  },
  "scripts": {
    "example": "PATH=$PATH:. node example.js",
    "debug-example": "PATH=$PATH:. node --inspect-brk example.js",
    "test": "PATH=$PATH:. jest test.js"
  }
}
